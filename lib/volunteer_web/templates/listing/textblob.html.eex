<div class="listing-textblob">
	<h5 class="textblob-section-title">About the Program</h5>
	<%= transform_textblob_content @listing.program_description %>

	<h5 class="textblob-section-title">About the Role</h5>
	<%= transform_textblob_content @listing.responsibilities %>

	<h5 class="textblob-section-title">About You</h5>
	<%= transform_textblob_content @listing.qualifications %>

	<%= if assigns[:time_commitment] do %>
		<h5 class="textblob-section-title mb-1">Time Commitment</h5>
		<%= render "meta.html", listing: @listing, show: [:time_commitment, :start_date_and_end_date] %>
		<!-- {{ with .Params.commitments }}
			<p>Your commitment will include:</p>
			<ul>
				{{ range . }}
					<li>{{ . }}</li>
				{{ end }}
			</ul>
		{{ end }} -->
	<% end %>
</div>
