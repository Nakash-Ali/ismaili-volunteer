<h2>Edit Listing</h2>

<%= render "form.html",
  changeset: @changeset,
  action_path: admin_listing_tkn_listing_path(@conn, :update, @listing, @tkn_listing) %>

<p><%= link "Back", to: admin_listing_tkn_listing_path(@conn, :show, @listing, @tkn_listing) %></p>
